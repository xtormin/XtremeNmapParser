# Runtime dependencies. The canonical list lives in pyproject.toml; this file
# is kept for `pip install -r requirements.txt` workflows.
pandas>=2.0,<3
lxml>=4.9
confuse>=2.0
xlsxwriter>=3.0
rich>=13.7
requests>=2.28
